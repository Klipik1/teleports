local http_request = http_request or request or HttpPost or syn.request or http.request 
local body = http_request({Url = 'https://httpbin.org/get'; Method = 'GET'}).Body;
local decoded = game:GetService('HttpService'):JSONDecode(body)
local hwid_list = {"fingerprint", "Krnl-Hwid", "shadow_hardware", "Syn-Fingerprint", "Exploit-Guid", "Proto-User-Identifier", "Sentinel-Fingerprint", "Fingerprint", "SW-Fingerprint", "Sw-Fingerprint", "Flux-Fingerprint"};
local hwid = "";    

getgenv().allowedkeys = {
    "50932fa6b23dc4711d2d2acfceae9046"
}

for i, v in next, hwid_list do
    if decoded.headers[v] then
        if key == decoded.headers[v] then
            for _,dang in pairs(allowedkeys) do
                if key == dang then
                    print("Whitelisted")
                else
                    print("SO EZ DUMB RETARD")
                    LocalPlayer:Kick("LOL NO WHITELISTED SKID")
                end
            end
        else
            print("LOL GOFFY ASS RETARD MONKEY")
            LocalPlayer:Kick("LOL NOT WHITELISTED SKID TRYIED TO START MY GODDEST SCRIPTS XDDDDDDDDDD")
        end
    end
end
