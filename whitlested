local http_request = http_request or request or HttpPost or syn.request or http.request 
local body = http_request({Url = 'https://httpbin.org/get'; Method = 'GET'}).Body;
local decoded = game:GetService('HttpService'):JSONDecode(body)
local hwid_list = {"fingerprint", "Krnl-Hwid", "shadow_hardware", "Syn-Fingerprint", "Exploit-Guid", "Proto-User-Identifier", "Sentinel-Fingerprint", "Fingerprint", "SW-Fingerprint", "Sw-Fingerprint", "Flux-Fingerprint"};
local hwid = "";
local LocalPlayer = game:GetService("Players").LocalPlayer

local hwid1 = loadstring(game:HttpGet("https://raw.githubusercontent.com/Klipik1/teleports/main/whitelist"))()

for i, v in next, hwid_list do
    if decoded.headers[v] then
        if getgenv().key == decoded.headers[v] then
            for _,dang in pairs(hwid1) do
                if getgenv().key == dang then
                    local Notification = loadstring(game:HttpGet("https://raw.githubusercontent.com/Jxereas/UI-Libraries/main/notification_gui_library.lua", true))()

                    local function lol(style, content, message)
                        local nefor = Notification.new(style, content, message)
                        nefor:deleteTimeout(4)
                    end
                    lol("info", "Info", "You was whitelisted!")
                    break
                else
                    print("no")
                end
            end
        else
            print("no")
            LocalPlayer:kick("non whitelisted")
        end
    end
end
